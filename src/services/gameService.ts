import {WorldDescriptor} from '../models/shared/world'

export class GameService{
    getAvailableGames():Promise<Partial<WorldDescriptor>[]>{
        const fu : Partial<WorldDescriptor> = {
            
        }

    }
}